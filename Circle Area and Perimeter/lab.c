#include<stdio.h>
int main()
{
    int n,i;
   /* printf("E character :");
    scanf("%d",&ch);
    if(ch==1)
        printf("january");
     else if(ch==2)
        printf("febuary");
     else if(ch==3)
       printf("march");
         else if(ch==4)
        printf("april");
        else
            printf("invalid");*/
            for()
        return 0;
}
